package com.eurlanda.datashire.schedule;

import com.eurlanda.datashire.engine.dao.ScheduleDao;
import org.junit.Test;
import org.springframework.beans.factory.annotation.Autowired;

/**
 * Created by zhudebin on 14-10-31.
 */
public class ScheduleDaoTest {

    @Autowired
    private ScheduleDao scheduleDao;

    @Test
    public void testJobScheduleSearch() {
        /**
        ApplicationContext context = new ClassPathXmlApplicationContext(new String[] { "conf/applicationContext.xml" });
        ConstantUtil.context = context;
        scheduleDao = context.getBean(ScheduleDao.class);
           */
        /*List<JobSchedule> jss = scheduleDao.getJobScheduleList();
        System.out.println(jss.size());
        Assert.assertNotNull(jss);*/
    }

    public ScheduleDao getScheduleDao() {
        return scheduleDao;
    }

    public void setScheduleDao(ScheduleDao scheduleDao) {
        this.scheduleDao = scheduleDao;
    }
}
